name: GitHub Packages Admin
description: Administer GitHub Packages
inputs:
    operation:
      required: true
runs:
  using: docker
  image: shaneapowell/ghaction-package-admin@DEVELOP
  args:
    - --operation ${{ inputs.operation }}
